server:
  port: 9001

spring:

  application:
    name: autorizacaoeusuarios

  # Database Configuration
  datasource:
    url: jdbc:postgresql://localhost:5432/autorizacaoeusuarios-db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
