server:
  port: 9000

spring:

  application:
    name: agendamentoseconsultas

  # Database Configuration
  datasource:
    url: jdbc:postgresql://localhost:5432/agendamentoseconsultas-db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
